pipeline {
    agent any

    stages {
        stage('Check Java Version') {
            steps {
                // On Windows use 'bat' instead of 'sh'
                bat 'java -version'
            }
        }

        stage('Build') {
            steps {
                bat 'mvn clean install -DskipTests'
            }
        }
    }
}
